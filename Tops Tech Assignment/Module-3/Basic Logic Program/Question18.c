// Calculate person’s Annual salary

#include<stdio.h>
int main() {
    int s,total;
    printf("Enter the monthly salary of a person: ");
scanf("%d", &s);
total = s * 12;
printf("The annual salary of a person is: %d", total);
}
